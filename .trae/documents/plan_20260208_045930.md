我将按照您的要求进行进一步的布局和样式调整：

1. **布局扩展（边缘移动）**：

   * 进一步增大负边距（Margin），使信号灯和数字更贴近左右边缘。

   * 计划将信号灯的左边距调整为 `-16`。

   * 计划将数字的右边距调整为 `-16`。

2. **数字颜色调整**：

   * 在 `CharacterGridItemModel.cs` 中新增一个 `ModCountBrush` 属性。

   * 修改逻辑：

     * **启用 Mod (Enabled > 0)**：设置为 **蓝色** (例如 `DodgerBlue` 或类似亮蓝色)。

     * **未启用 Mod (Enabled == 0)**：保持 **灰色**。

   * 数字颜色不再跟随信号灯（红/绿）变化。

3. **代码实现**：

   * 修改 `CharacterGridItemModel.cs` 添加新属性和逻辑。

   * 修改 `CharactersPage.xaml` 绑定新的 `ModCountBrush`，并调整 Margin。

