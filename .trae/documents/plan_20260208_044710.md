确实，目前的效果还有很大的优化空间。根据您的反馈，我将进行以下针对性的改进：

1. **数字显示优化**：

   * 增大字体大小，使其更清晰。

   * 将数字颜色与信号灯颜色同步（绿色/红色/灰色），增强视觉关联性。

   * 将数字放置在信息行的右下角，充分利用右侧空间。

2. **信号灯与模糊效果优化**：

   * 信号灯放置在信息行的左上角（垂直居中）。

   * 改进发光效果：使用 `CommunityToolkit.WinUI.UI.Media.Effects` 中的 `DropShadow` 或更高级的 `Blur` 效果（如果项目支持），或者通过多层叠加不同透明度和大小的 `Ellipse` 来模拟更柔和、自然的辉光，避免简单的边缘生硬感。目前项目已引入 CommunityToolkit，我将尝试使用阴影效果来模拟发光。

3. **布局调整**：

   * 保持图片层纯净。

   * 下方信息行采用 Grid 布局：

     * 左列：信号灯（带发光）

     * 中列：角色名（居中）

     * 右列：Mod 数量（右对齐，带颜色）

具体实施计划：

1. 修改 `CharacterGridItemModel.cs`，确保 `StatusBrush` 逻辑正确（已完成，但需确认是否需要为字体单独定义 Brush，目前可以直接复用 StatusBrush）。
2. 修改 `CharactersPage.xaml`，重写 ItemTemplate 的下方 Grid 部分，应用上述布局和样式调整。
3. 构建并运行测试。

