我将通过以下步骤来实现您的需求：

1. **修改数据模型 (`CharacterGridItemModel.cs`)**：

   * 新增 `StatusBrush` 属性，用于控制“信号灯”的颜色。

   * 更新 `SetMods` 方法逻辑：

     * 当启用 Mod 数量为 0 时，信号灯为**灰色**。

     * 当启用 Mod 数量为 1 时，信号灯为**绿色**。

     * 当启用 Mod 数量 >= 2 时，信号灯为**红色**。

