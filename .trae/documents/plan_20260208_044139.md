我将执行以下修改以满足您的需求：

1. **优化信号灯颜色**：在 `CharacterGridItemModel.cs` 中，将信号灯的颜色调整为更鲜艳、透亮的颜色（如荧光绿、亮红），以增强视觉效果。
2. **重构卡片布局**：在 `CharactersPage.xaml` 中，修改角色卡片的布局结构。

   * 将**信号灯**和**数量显示**从图片区域移出，不再遮挡立绘。

   * 在立绘下方创建一个新的信息行，包含：左侧的信号灯、中间的角色名、右侧的数量显示。
3. **实现发光效果**：通过叠加半透明层的方式，为信号灯添加“边缘模糊/发光”的视觉效果，使其看起来更具质感。

具体步骤：

1. 修改 `CharacterGridItemModel.cs` 更新颜色逻辑。
2. 修改 `CharactersPage.xaml` 调整 `AdaptiveGridView.ItemTemplate` 的 XAML 结构。

